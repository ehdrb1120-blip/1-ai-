# CLAUDE.md — 당근마켓 위탁판매 × 인스타 유입 LLM Wiki

## 1. Vault 정체성

**도메인**: 당근마켓 위탁판매 × 인스타 유입 1인 이커머스

| 영역 | 정의 (1줄) |
|---|---|
| `sourcing` | 무엇을 팔까 — 위탁 공급처, 제품 후보, 원가·마진, 경쟁사/시세 리서치 |
| `selling` | 어떻게 팔까 — 당근 상품 등록글·상세페이지 카피, 제목/가격 실험, CS 문의 대화와 응대 스크립트 |
| `marketing` | 어떻게 알릴까 — 인스타 게시물 기획·카피·해시태그, 인스타→당근 유입 동선 |
| `ops` | 운영/shared — vault 헌법, 슬래시 커맨드 매뉴얼, 플랫폼 정책·배송·정산 체크리스트 |

## 2. 3레이어

| 레이어 | 경로 | 성격 | LLM 권한 |
|---|---|---|---|
| raw | `raw/` | 불변 원천 (원본 대화·스샷·리서치 덤프) | **읽기 전용. 수정·삭제 절대 금지** |
| wiki | `wiki/` | LLM이 정제·압축한 지식 | 읽기/쓰기 |
| 스키마 | `CLAUDE.md` | 구조·규칙 정의 | 사용자 승인 시에만 수정 |

## 3. Operations

- `/ingest` — raw 자료를 진입 판정 → 영역 분류 → 압축하여 wiki에 박제
- `/query` — wiki를 읽기 전용으로 큐레이션 (합성 답변 + 관련 노트 + 다음 액션)
- `/lint` — wiki 건강 진단 (모순·고립 노트·데이터 갭·미처리 raw)

## 4. raw→wiki 진입 규칙 (요약)

Q1 도메인 적합성(`당근마켓` · `위탁판매` · `인스타 유입` · `1인 이커머스`) → Q2 재사용 가치 → Q3 검증 가능성 순으로 판정하며 결과는 **통과 / 참조 / 모호 / 반려** 4리프.
전체 트리는 [[raw-wiki-규칙]] §A 참조.

## 5. 영역 분류 결정 트리 (요약)

```
제품을 "고를 때" 쓰는 정보인가?            → sourcing
아니면 구매 직전 고객이 보거나 나누는 것?   → selling
아니면 고객을 "데려오는" 것?                → marketing
아니면 판매 자체가 아닌 운영·규칙·시스템?   → ops
그 외 / 둘 이상에 걸침                      → [모호] 사용자 개입
```
전체 트리는 [[raw-wiki-규칙]] §B 참조.

## 6. 압축 룰 (요약)

raw 1000줄 → wiki 100~200줄(10~20%), 노트 상한 200줄, raw 1자료당 직접 인용 ≤3줄, cross-link ≥3개(그중 타 영역 ≥1개).
수치 표는 [[raw-wiki-규칙]] §C 참조.

## 7. Frontmatter 표준

모든 wiki 노트는 아래 4키를 **의무** 포함한다.

```yaml
---
area: sourcing | selling | marketing | ops
created: YYYY-MM-DD
sources:
  - "[[raw 노트명]]"       # 반드시 큰따옴표. 출처 없으면 "직접작성"
tags: [소싱, 마진]
---
```

- `area` — 결정 트리 §B 결과값. 4개 중 하나만.
- `created` — 노트 최초 생성일 (수정일 아님).
- `sources` — **위키링크 배열**. 참조 판정 자료도 여기 기록.
  - 경로 문자열(`raw/…/파일.md`)로 쓰면 **그래프뷰에 선이 안 그어져 wiki와 raw가 끊긴다.** 반드시 `"[[노트명]]"` 형식으로 쓴다.
  - YAML 특성상 큰따옴표를 빼면 중첩 배열로 잘못 파싱되니 **따옴표 필수**.
  - 출처가 없는 자체 작성 노트만 `직접작성`(따옴표 없이).
- `tags` — 소문자/한글 자유, 3~7개 권장.

## 8. 스킬 매뉴얼 규칙

`.claude/skills/`에 새 스킬이 추가되면 **`wiki/ops/스킬-{이름}.md` 매뉴얼 노트를 반드시 함께 만든다.**
스킬 본체는 점(`.`)으로 시작하는 폴더에 있어 옵시디언이 인식하지 못하므로, 그래프와 검색에 남기려면 ops 영역에 매뉴얼이 필요하다. 매뉴얼에는 호출법·역량·한계·연결 노트를 적는다.
